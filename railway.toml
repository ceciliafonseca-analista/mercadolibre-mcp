[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npx -y supergateway --stdio \"node dist/index.js\" --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
